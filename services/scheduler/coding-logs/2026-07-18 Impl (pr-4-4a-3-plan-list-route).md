# PR 4.4a-3 — control-plane plan LIST route + contracts + CI (impl log)

**Date:** 2026-07-18/19 · **Base:** main `64b8f9d5` (post-4.4a-2) · **Branch:** `feat/control-plane-plan-list`
**Plan:** `coding-logs/2026-07-18 Plan (pr-4-4a-4-4b-control-plane-hardening).md` (sub-PR 4.4a-3). Scheduler + bff + contracts + CI. Final sub-PR of the split PR 4.4a.

## What shipped
- **Scheduler `GET /api/v1/control-plans`** (`require_operator`): cursor-paginated, filterable, BOUNDED plan summary list. NEW `core/control_plan_cursor.py` (opaque base64url canonical-JSON cursor {v:1, created_at UTC, plan_id, plan_version, filters_sha256}; strict-validated → CursorError→422 on malformed/unknown-version/non-UTC/non-UUID/filter-mismatch) + NEW `repositories/control_plan_projection_repository.py` (`list_plan_summaries` — header-columns-only SELECT, NEVER `_assemble`; keyset `(created_at, plan_id, plan_version) < cursor` over the TOTAL row identity, `ORDER BY created_at DESC, plan_id DESC, plan_version DESC`, fetch limit+1). Derived `lifecycle_state` + `approval_trust` come from TWO correlated scalar subqueries in the SAME statement (one MVCC snapshot → filter and returned state are consistent); the document subquery is restricted to the single `shadow_approved` transition (bounded). `ControlPlanSummaryOut` (extra="forbid") carries NO large document. Additive index migration `0004_control_plan_list_indexes` (`(created_at DESC, plan_id DESC, plan_version DESC)` + narrow filter indexes; index-only, drift-lock unaffected).
- **BFF list pass-through** `GET /api/v1/control-plans`: bearer-forwarded, strict-mirror validated (extra="forbid" + `projection_schema_version: Literal[1]` → upstream v2 = 502), preserves the scheduler failure taxonomy, never fabricates an empty page; uses the pooled lifespan client. New `SchedulerClient.list_control_plans`.
- **Contracts** `contracts/control-plans/v1/control-plan-list-page.{schema,example}.json` validated by BOTH scheduler + bff (drift → test failure). **Dormant CI** `.github/workflows/control-plane-hardening-tests.yml` (scheduler+bff pytest + ephemeral-Postgres migration roundtrip; tracked via a `.gitignore` negation; dormant under E6).

## Gate
scheduler **420 passed / 10 skip** (baseline 379+6; +41 tests, +4 env-gated Postgres skips), bff **133** (baseline 101; +32) — each ×3, no new unconditional skips, nothing weakened. pyflakes clean. ORM↔DDL drift-lock (26p) unaffected (0004 is index-only).

## 2-tier QCHECK — the two tiers DISAGREED; adjudicated on merits
- Tier-1 (Opus adversarial): verified the whole attack surface SOLID (keyset dup/gap, SQLi, RBAC, large-doc exclusion, BFF fail-closed, migration index-only, contract non-tautology); found 1 MEDIUM (BFF masks scheduler 422→502) + 2 LOW.
- Tier-2 (Codex gpt-5.6-sol high): 3 HIGH (keyset not total / two-snapshot state race / accepts version 999) + 3 MED (cursor forgeable→HMAC / unbounded doc load / CI not tracked) + 1 LOW (base64 not strict).
- **Adjudication + fixes (8):** (1) added `plan_version` to keyset/cursor/order/index [Codex HIGH — latent but closed]; (2) folded state+trust into ONE snapshot statement [Codex HIGH]; (3) load only the shadow_approved doc + cap `reason`(2000)/`evidence_refs`(200) length [Codex MED]; (4) `projection_schema_version: Literal[1]` both sides → v2=502 [Codex HIGH]; (5) **DECLINED the cursor HMAC** — adjudicated per Opus that the cursor is a non-security continuity token (forging only repositions the forger's own RBAC+filter-gated view; kept `filters_sha256` for accidental-reuse; added `isinstance(version,int)` + equal-created_at no-dup/gap test) [Codex MED — declined w/ rationale]; (6) strict base64url validation [Codex LOW]; (7) `.gitignore` negation so the workflow is tracked [Codex MED]; (8) map scheduler 400/422 → `SchedulerBadRequestError` → BFF 422/400 (stop masking client errors as 502) [Opus MED]. Each fix regression-tested.
- Adaptation: the LIST derives state from the max-sequence `to_state` (not full-chain `derive_control_plan_state`) to stay bounded — sound because write-time integrity (0003 edge-graph CHECK + immutability triggers) guarantees a valid append-only chain; zero-transitions still 503. The detail path still fully validates.

## External ops (unchanged)
Apply migration 0004 (index-only, additive) to the remote via the controlled cutover; restore E6 CI billing to activate the dormant workflow.

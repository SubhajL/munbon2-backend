[pytest]
# Discovery is confined to tests/ — operational probes live in scripts/ops/
# and must never be collected (PR 4.2).
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --strict-markers
asyncio_mode = strict
markers =
    asyncio: Asynchronous tests

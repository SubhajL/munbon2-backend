# PR 6.1b — Approved Field Device & Quantizer Artifact (IMPL LOG)

**Branch:** `feat/6-1b-approved-field-artifact` off `origin/main` (`10711d2f`). **Service:** `services/scada-gate-control` (TS).
**Plan:** `coding-logs/2026-07-20 Plan (pr-6-1b-approved-field-artifact).md` (Claude + Plan-agent 2nd opinion synthesis; Codex out of tokens).
**Gate:** `npm run typecheck && npm run lint && npm test` — clean. Suite **355 passed / 4 skipped** (baseline 303 → +52 new; 32 artifact + 12 anchor-in-validation + 2 route + 8 ops CLI... net +52 incl. helper counts). Build clean. 3× consecutive clean.

## What shipped
SCADA-side, software-complete (D6 unavailable → fixtures only; every new runtime path dark-by-default, so an un-configured service is byte-identical to 6.2). NO actuator/transport/Modbus/network path introduced.

1. **`src/domain/approved-field-artifact.schema.ts`** — embedded `APPROVED_FIELD_ARTIFACT_SCHEMA_V1` (rich: canonical gate + device/adapter + register/unit + discrete quantizer + readback semantics + evidence refs + approval metadata + 3-field `approved_lineage_anchor`) + `APPROVED_LINEAGE_ANCHOR_SCHEMA_V1` (the standalone runtime anchor). NOT added under `contracts/machine-boundary/v1/` (would break the FROZEN `MACHINE_BOUNDARY_CONTRACT_SET_SHA256`); embedded like 6.1a's snapshot schema.
2. **`src/domain/approved-field-artifact.ts`** — pure: `parseApprovedFieldArtifact` (Ajv + hygiene: reserved gate ids + `://`/`@` on id fields, reusing the 6.1a `RESERVED_GATE_KEYS`/`ENDPOINT_OR_CREDENTIAL` primitives now exported from `device-registry.ts` — no fork); `buildDeviceRegistryArtifact` (projects → EXACTLY `{capability_release_id, capabilities:{gate:{device_id,adapter_gate_id,targets}}}`; gates sorted by id, targets by `target_level`; built via `Object.fromEntries` so a hostile gate id can't pollute the prototype); `validateApprovedRegistryCoverage` (exact expected-scope, no dup, `assertQuantizerMonotone` = strictly-increasing position AND level, `assertReadbackSeparable` = adjacent positions > 2× tolerance); `extractApprovedLineageAnchor`; `loadApprovedLineageAnchor` (dark-by-default, 64KiB cap, Ajv-validated, throws when set-but-broken); `lineageMatchesAnchor` (exact 3-field equality).
3. **`src/domain/command-intent-validation.ts`** — `validateCommandIntent` gains an optional 4th arg `approvedLineageAnchor = null`; `lineage_mismatch` at position 4 (freshness → capability → target → **lineage** → window → deadline). Purely additive: null (default) → byte-identical to 6.2.
4. **`src/ops/build-device-registry.ts`** — OFFLINE CLI (`npm run build-device-registry <artifact.json> [gateId...]`): `projectApprovedArtifact` (parse → coverage → project) + `main` (argv/IO shell, fail-closed nonzero exit + no output on violation). Gives the generator a runnable home (not orphaned).
5. **Anchor decision (3 fields):** `{model_release_id, model_release_content_hash, engine_descriptor_content_hash}` — the "approved commandable release" identity, stable across plan versions. EXCLUDES per-plan/per-run fields (campaign/plan/prediction/artifact) so a new plan version under the same approved model release is NOT rejected. (2nd-opinion proposed 4 fields incl. `artifact_sha256`; rejected — per-run, would reject legitimate versions. Documented + tested.)
6. **Wiring:** `loadApprovedLineageAnchor()` in `index.ts` (dark warn when null) → `ApiDeps.approvedLineageAnchor` (routes.ts) → `InternalDeps` Pick (internal-routes.ts) → `validateCommandIntent` 4th arg. `server.ts` unchanged (spreads one deps object). `services/scada-gate-control/CLAUDE.md` documents the env + offline tool + D6-blocked note.

## Empirical end-to-end verification (offline operator flow)
Ran the real CLI on the example artifact: two independent runs BYTE-IDENTICAL (the byte-exact done-gate); the projected registry loads clean through the REAL 6.1a `loadDeviceCapabilitySnapshot` with `capability_hash db0fa00a8cfaf6b0f350bbead0628e3f7dc7a3d3f2c4f267944c91a7e9fd43f5` matching the independent `computeCapabilityHash` oracle; the anchor round-trips through the REAL `loadApprovedLineageAnchor`; dark default (no env) → `null` (6.2-identical).

## Flakiness note
One rare (~1/60) full-suite failure reproduced ONCE during the first 3× check and never again (60+ subsequent full runs clean; my new specs 25/25 clean in isolation, no timers/shared state) → a pre-existing rate-limit/supertest timing flake, NOT introduced by 6.1b. Final 3× consecutive clean confirmed.

## 2-tier QCHECK — NO CRITICAL, NO HIGH (both tiers)
Tier-1 `/code-review high` (workflow, 15 agents, 0 errors) + Tier-2 Opus 4.8 adversarial (Codex out of tokens; prompt = the goal's developer-feedback framing). The two tiers were uncorrelated (as designed): Opus independently RE-DERIVED the same `capability_hash db0fa00a…` and verified fail-closed/dark/no-Modbus; the workflow surfaced the idempotency-replay + whitespace-path angles Opus didn't. Resolutions:
- **[tier-1 C1, PLAUSIBLE] idempotent replay bypasses a newly-armed anchor** — a 6.2 receipt is frozen at first validation; a receipt minted while dark replays `validation_accepted` after arming. NOT a code change (special-casing the anchor would fork the immutable-receipt idempotency contract, which applies to ALL validation inputs). DOCUMENTED as a deploy note (arm anchor ⇒ rotate the `scada_command_intents` receipt store; done at the external trust cutover on a fresh store) — in the loader docstring + `services/scada-gate-control/CLAUDE.md`.
- **[tier-1 C2, PLAUSIBLE] whitespace anchor path silently ran dark** — FIXED: env var UNSET (undefined) → dark; SET-but-blank/whitespace → THROW (opposite safety valence to 6.1a's safe zero-gates default; honors the "never silently disabled" contract). Test flipped + `''` case added.
- **[tier-1 C3, CONFIRMED] duplicated reserved-key guard in build** — already resolved by the M1/L1 refactor (folded into `assertArtifactHygiene`).
- **[tier-1 C4, PLAUSIBLE] forked capped-JSON loader** — FIXED: extracted `src/domain/capped-json-file.ts` `readCappedJsonFile` (statSync-cap → read → JSON.parse, error-factory param); BOTH `loadDeviceCapabilitySnapshot` (6.1a, behavior-preserving — its tests still green) and `loadApprovedLineageAnchor` now call it. +5 direct tests.
- **[tier-2 M1] projection silently dropped a device on duplicate gate id** — FIXED: `buildDeviceRegistryArtifact` is now SELF-SAFE (calls `assertArtifactHygiene` + shared `assertUniqueGateIds`), so it no longer depends on the caller having run coverage. +3 self-safety tests.
- **[tier-2 N1] non-total gate comparator** — FIXED (`compareGateId` returns 0 on equal). **[tier-2 L1] hygiene only in parse** — FIXED (build re-runs it). **[tier-2 L2] anchor↔registry binding unobservable** — FIXED (startup log of `capability_release_id` + anchor `model_release_id` when armed). **[tier-2 N2]** clarifying comment on the two equivalent sort keys. **[tier-2 N3]** tightened 2 matchers to the specific guard.
- **[tier-2 M2 + L3] DEFERRED (documented in CLAUDE.md):** runtime-loader re-check of the quantizer invariants (needs real D6 + the 6.3 reconciler); randomized TS↔Python `canonicalize` differential test (reachable surface is ASCII-only, golden-vector-guarded).
- **Design questions both tiers validated:** the 3-field anchor granularity (stable across plan versions, forces re-approval on model/engine content change — "not too loose, not too tight") and the position-4 ordering (never masks/masked in either direction).

Post-fix gate: typecheck/lint/build clean, **363 passed / 4 skipped**, 3× consecutive clean, wiring verified (`readCappedJsonFile` used by both loaders).

## Deploy note
6.1b is fully DARK: `SCADA_APPROVED_LINEAGE_ANCHOR_PATH` unset → the `lineage_mismatch` check is a no-op (6.2-identical). The real approved artifact (with RID-approved D6 device/register/quantizer data) remains EXTERNAL — downstream real shadow dispatch stays blocked until D6 is available, exactly per the roadmap done-gate.
